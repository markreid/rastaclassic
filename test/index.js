/* eslint-env mocha */

/**
 * unit tests
 */

const { assert } = require('chai');

const util = require('../lib/util');

describe('a test', () => {
  it('runs', () => {
    assert(true);
  });

  it('fails', () => {
    assert(false);
  });
});
